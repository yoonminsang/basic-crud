// import CustomError from './custom-error';

// const errorGenerator = (message: string, customMessage?: string): CustomError => {
//   return new CustomError(message, customMessage);
// };

// export default errorGenerator;
